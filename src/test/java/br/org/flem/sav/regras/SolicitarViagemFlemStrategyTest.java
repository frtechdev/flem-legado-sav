
package br.org.flem.sav.regras;

import junit.framework.TestCase;

/**
 *
 * @author mccosta
 */
public class SolicitarViagemFlemStrategyTest extends TestCase {
    
    public SolicitarViagemFlemStrategyTest(String testName) {
        super(testName);
    }
    
    @Override
    protected void setUp() throws Exception {
        super.setUp();
    }
    
    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    
    public void testFuncionarioPodeViajar() {
        /*System.out.println("funcionarioPodeViajar");
        
        IFuncionario iFunc = new HRBIImpl().obterFuncionarioPorMatricula(947);
        
        ArrayList<String> resposta = new ArrayList<String>();
        resposta.add("false");
        resposta.add("O Funcionário \"" + iFunc.getNome().replace("  ", "") + "\" não pode solicitar uma viagem porque ainda não prestou conta da(s) seguinte(s) viagem(ns), ID(s) ref.: \"12712, 12622, 12607\"! ");
        resposta.add("false");
        
        SolicitarViagemFlemStrategy instance = new SolicitarViagemFlemStrategy();
        
        ArrayList expResult = resposta;
        ArrayList result = instance.funcionarioPodeViajar(iFunc);

        assertEquals(expResult, result);*/
    }
}
